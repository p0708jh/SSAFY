const nums = [1, 2, 3, 4]

const newNums=nums.map(e=>e*e*e);

// `map`
// 하나씩 순회하며 리턴
// `filter`
// 하나씩 순회하며 조건에 맞는거만 리턴
// `find`
// find의 해당하는 값을 리턴
// `every`
// 배열의 모든 원소가 존재하는지
// `some`
// 배열의 적어도 하나의 원소가 존재하는지
// `reduce`
// acc와 cur로 이루어진 누산기 
